export const DAY_TEMPLATE = `import { lines } from "../../utils";

export function part1(input: string): number | string {
  const data = lines(input);
  return 0;
}

export function part2(input: string): number | string {
  const data = lines(input);
  return 0;
}
`;

